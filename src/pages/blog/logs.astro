---
import PostCard from "../../components/PostCard.astro";
import { blogSections } from "../../data/site";
import Layout from "../../layouts/Layout.astro";
import { getBlogBySection } from "../../lib/content";

const section = blogSections.find((item) => item.key === "field-logs");
if (!section) {
  throw new Error("Missing blog section: field-logs");
}
const posts = await getBlogBySection("field-logs");
---

<Layout title={section.title} description={section.description}>
  <header class="page-header">
    <p class="eyebrow">{section.intensity} intensity</p>
    <h1>{section.title}</h1>
    <p>{section.description}</p>
  </header>

  <section class="page-section" aria-labelledby="logs-list-title">
    <div class="section-heading">
      <div>
        <h2 id="logs-list-title">Published logs</h2>
      </div>
    </div>
    {
      posts.length > 0 ? (
        <div class="post-list">
          {posts.map((post) => (
            <PostCard post={post} />
          ))}
        </div>
      ) : (
        <p class="empty-state">
          Field logs will collect migrations, experiments, incidents, and build notes.
        </p>
      )
    }
  </section>
</Layout>
