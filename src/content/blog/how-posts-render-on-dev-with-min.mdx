---
title: "How Posts Render on Dev with Min"
description: "Markdown, MDX callouts, Mermaid diagrams, code blocks, article layout."
publishedAt: 2026-07-06
section: field-logs
tags:
  - meta
  - blog
  - mdx
  - astro
  - components
draft: false
---

import Callout from "../../components/blog/Callout.astro";
import Danger from "../../components/blog/Danger.astro";
import Decision from "../../components/blog/Decision.astro";
import Mermaid from "../../components/blog/Mermaid.astro";
import Note from "../../components/blog/Note.astro";
import Question from "../../components/blog/Question.astro";
import Reference from "../../components/blog/Reference.astro";
import Tip from "../../components/blog/Tip.astro";
import Todo from "../../components/blog/Todo.astro";
import Warning from "../../components/blog/Warning.astro";

## Purpose

Rendering guide, MDX components, Mermaid diagrams, code blocks, article layout.

Regression reference, component checks, public-layout inspection.

<Note title="Live reference">
  Published preview, real layout, production-facing component checks.
</Note>

## Markdown baseline

Readable width, inline commands, keyboard tokens, low visual noise.

Blockquote baseline:

> Practical context, tradeoffs, future questions, technical detail.

List baseline:

- short observations
- implementation notes
- commands worth keeping
- follow-up questions

## Callouts

Stable visual accents for context, decisions, warnings, follow-up work.

<Note title="Note">Context, reminders, background, supporting detail.</Note>

<Tip title="Tip">Small improvements, safe shortcuts, repeatable habits.</Tip>

<Question title="Question">Open questions, assumptions, design pressure.</Question>

<Decision title="Decision">Current position, tradeoff, reason to proceed.</Decision>

<Reference title="Reference">Docs, papers, source links, related posts.</Reference>

<Warning title="Warning">Tradeoffs, migration hazards, hidden assumptions.</Warning>

<Danger title="Danger">Data loss, downtime, security exposure, irreversible operations.</Danger>

<Todo title="Todo">Follow-up experiments, future posts, revisit checks.</Todo>

<Callout variant="tip" title="Base Callout">
  Inline variant control, wrapper-free callout usage.
</Callout>

## Mermaid diagrams

Local Mermaid component, titled diagrams, scrollable figures.

<Mermaid
  title="Blog flow"
  chart={`
flowchart LR
  Experience[Experience] --> Draft[Draft]
  Draft --> Review[Review]
  Review --> Publish[Publish]
  Publish --> Revisit[Revisit]
  Revisit --> Draft
`}
/>

<Mermaid
  title="Request sequence"
  chart={`
sequenceDiagram
  participant Reader
  participant Browser
  participant Blog
  Reader->>Browser: Open post URL
  Browser->>Blog: Request static page
  Blog-->>Browser: Return HTML, CSS, and JS
  Browser-->>Reader: Render post
`}
/>

<Mermaid
  title="Content model"
  chart={`
erDiagram
  AUTHOR ||--o{ POST : writes
  POST ||--o{ TAG : has
  AUTHOR {
    string id PK
    string name
  }
  POST {
    string id PK
    string title
    datetime published_at
  }
  TAG {
    string slug PK
    string name
  }
`}
/>

<Mermaid
  title="Post lifecycle"
  chart={`
stateDiagram
  [*] --> Draft
  Draft --> Published: publish
  Published --> Revised: learn more
  Revised --> Published: update
`}
/>

<Mermaid
  title="Site records"
  chart={`
classDiagram
  class BlogPost {
    +string title
    +string section
    +string[] tags
    +boolean draft
  }
  class ProjectRecord {
    +string name
    +string status
  }
  class DeveloperProfile {
    +string focus
  }
  DeveloperProfile --> BlogPost : authors
  BlogPost --> ProjectRecord : references
`}
/>

<Mermaid
  title="Revision history"
  chart={`
gitGraph
  commit id: "draft"
  branch revise
  checkout revise
  commit id: "clarify"
  checkout main
  merge revise
  commit id: "publish"
`}
/>

## Code blocks

Expressive Code, titles, terminals, line highlights, labels, diffs, logs.

```ts title="src/lib/example.ts" {"watch this":3-6}
type BlogSection = "light-notes" | "field-logs" | "deep-dives";

export function getSectionLabel(section: BlogSection) {
  return section.replace("-", " ");
}
```

```bash title="Terminal"
bun run check
bun run check:public
```

```ts title="src/content.config.ts" del={1} ins={2}
draft: z.boolean().default(true),
draft: z.boolean().default(false),
```

```nix title="flake.nix" {3}
packages.default = pkgs.stdenvNoCC.mkDerivation {
  pname = "dev-with-min";
  version = "0.1.0";
};
```

```log title="build.log"
14:07:39 [build] Collecting build info...
14:07:40 [WARN] Some chunks are larger than expected
14:07:40 [build] Complete!
```

## Frontmatter shape

Schema-first posts, predictable metadata, RSS/social/archive coverage.

```yaml title="src/content/blog/example.mdx"
---
title: "Example Post"
description: "Listings, RSS, social previews."
publishedAt: 2026-07-06
section: field-logs
tags:
  - astro
  - mdx
draft: false
---
```

<Decision title="Current rendering contract">
  Plain Markdown baseline, MDX only for stronger visual shape.
</Decision>
