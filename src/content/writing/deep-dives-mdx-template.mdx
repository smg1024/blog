---
title: "Deep Dives MDX Template"
description: "TODO: Write a short summary."
publishedAt: 2026-07-06
section: deep-dives
tags:
  - tag1
  - tag2
  - tag3
draft: true
---

import Decision from "../../components/writing/Decision.astro";
import Danger from "../../components/writing/Danger.astro";
import Mermaid from "../../components/writing/Mermaid.astro";
import Note from "../../components/writing/Note.astro";
import Question from "../../components/writing/Question.astro";
import Reference from "../../components/writing/Reference.astro";
import Todo from "../../components/writing/Todo.astro";
import Warning from "../../components/writing/Warning.astro";
import Callout from "../../components/writing/Callout.astro";
import Tip from "../../components/writing/Tip.astro";

## Question

What is the main technical or philosophical question?

<Question title="Open question">
  Use this to name the uncertainty, hypothesis, or design question the post is
  trying to answer.
</Question>

<Note title="Use this for context">
  Put a short aside here when the reader needs setup, vocabulary, or a reminder
  before the deeper explanation.
</Note>

## Model

Explain the mental model, architecture, or principle.

<Decision title="Current position">
  State the technical choice or belief you are committing to in this post, then
  explain the reason.
</Decision>

## Details

Break down the implementation, examples, diagrams, or references.

<Mermaid
  title="System flow"
  chart={`
flowchart LR
  Input[Input] --> Process[Process]
  Process --> Output[Output]
`}
/>

<Mermaid
  title="Data model"
  chart={`
erDiagram
  AUTHOR ||--o{ POST : writes
  POST ||--o{ COMMENT : receives
  AUTHOR {
    string id PK
    string name
    string email
  }
  POST {
    string id PK
    string author_id FK
    string title
    datetime published_at
  }
  COMMENT {
    string id PK
    string post_id FK
    string body
  }
`}
/>

<Mermaid
  title="Request sequence"
  chart={`
sequenceDiagram
  participant Reader
  participant Browser
  participant Blog
  participant Homelab
  Reader->>Browser: Open post URL
  Browser->>Blog: Request static page
  Blog-->>Browser: Return HTML, CSS, and JS
  Browser->>Homelab: Load cached assets
  Homelab-->>Browser: Return assets
  Browser-->>Reader: Render post
`}
/>

<Mermaid
  title="Writing lifecycle"
  chart={`
stateDiagram
  [*] --> Draft
  Draft --> Review: clarify argument
  Review --> Draft: revise
  Review --> Published: publish
  Published --> Updated: learn more
  Updated --> Published: republish
`}
/>

<Mermaid
  title="Content types"
  chart={`
classDiagram
  class WritingPost {
    +string title
    +string section
    +string tags
    +boolean draft
  }
  class ProjectRecord {
    +string name
    +string status
    +string stack
  }
  class DeveloperProfile {
    +string focus
    +string location
    +string links
  }
  WritingPost --> ProjectRecord : references
  DeveloperProfile --> WritingPost : authors
`}
/>

<Mermaid
  title="Revision history"
  chart={`
gitGraph
  commit id: "draft"
  branch revise
  checkout revise
  commit id: "add context"
  commit id: "tighten model"
  checkout main
  merge revise
  commit id: "publish"
`}
/>

```ts title="src/lib/example.ts" {3-5}
type WritingSection = "light-notes" | "field-logs" | "deep-dives";

export function getSectionLabel(section: WritingSection) {
  return section.replace("-", " ");
}
```

```bash title="Terminal"
bun run check
bun run build
```

```ts title="src/content.config.ts" del={1} ins={2}
draft: z.boolean().default(true),
draft: z.boolean().default(false),
```

```nix title="flake.nix" {3}
packages.default = pkgs.stdenvNoCC.mkDerivation {
  pname = "dev-with-min";
  version = "0.1.0";
};
```

```log title="build.log"
10:50:26 [build] Collecting build info...
10:50:26 [WARN] Some chunks are larger than expected
10:50:26 [build] Complete!
```

<Callout variant="tip" title="Tip">
  Use the base `Callout` component directly when you want to choose the variant
  inline.
</Callout>

<Reference title="Useful reference">
  Link to docs, papers, source code, or earlier posts that support the
  explanation.
</Reference>

## Tradeoffs

What does this approach make easier, and what does it make harder?

<Warning title="Important tradeoff">
  Use this when a shortcut, risk, operational hazard, or misconception deserves
  extra attention.
</Warning>

<Danger title="Failure mode">
  Use this for data loss, security, downtime, or other high-impact risks.
</Danger>

## Conclusion

What do I believe now?

<Todo title="Follow-up">
  Capture the next experiment, missing measurement, or future post idea.
</Todo>
